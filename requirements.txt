# Core dependencies
PyYAML==6.0.1          # YAML configuration file handling
requests==2.31.0       # HTTP requests for API communication

# These versions are known to work well together and are security-patched
